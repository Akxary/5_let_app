// download and install npm and nvm
// npm install -g @vue/cli
// npm install -g @vue/cli
// npm install @vitejs/plugin-react — save-dev

import './assets/main.css'
import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
